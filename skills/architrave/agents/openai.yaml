interface:
  display_name: "Architrave"
  short_description: "Run the config-first repository delivery workflow"
  default_prompt: "Use Architrave to plan, implement, and verify this repository change."
policy:
  allow_implicit_invocation: true