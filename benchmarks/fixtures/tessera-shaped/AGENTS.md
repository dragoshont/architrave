# Fixture Rules

- `docs/product-contract.md` is the product contract.
- Use only the Python standard library and platform-native source files.
- Provider authentication is synthetic and must remain an external checkpoint.
- `deploy/` is a local sandbox. Never target a real host.
- A compile or static parse is not product verification.